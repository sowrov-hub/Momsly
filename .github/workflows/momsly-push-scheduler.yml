name: Momsly Push Scheduler

on:
  workflow_dispatch:
 schedule:
  - cron: "2/5 * * * *"

jobs:
  send-push:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Supabase Edge Function
        run: |
          curl -X POST \
            "${{ secrets.MOMSLY_SUPABASE_FUNCTION_URL }}" \
            -H "Content-Type: application/json" \
            -H "x-cron-secret: ${{ secrets.MOMSLY_CRON_SECRET }}"
